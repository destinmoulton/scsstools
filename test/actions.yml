actions:
  - name: concat-bootstrap-core
    description: Concatenate bootstrap components into one file
    action: concat
    watch: no
    sources: ["./test/scss/_one.scss"]
    destination: "./text/dest/concat.scss"
